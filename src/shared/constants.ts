export const URL_ROUTE = {
  HOME: '',
}
export const URL_PATH = {
  HOME: '',
}

export const windowExist = typeof window !== 'undefined'

export const REGEX = {
  OBJECT: /{.*}/g,
  STRING: /".+"/g,
  KEY_PATH: /'[a-zA-Z.$]+'/g,
}
